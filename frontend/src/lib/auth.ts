// Validate Auth
export const getToken = () => {
  return localStorage.getItem("token");
}

// Login
export const login = () => {
  return null
}

// Logout
export const logout = () => {
  return null;
}

